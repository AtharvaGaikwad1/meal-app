import 'package:flutter/material.dart';

class FavScr extends StatelessWidget {
  const FavScr({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('this is fav screen'),
    );
  }
}
